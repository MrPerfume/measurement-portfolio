# Contributing

这个仓库优先保持“小而可审计”。提交内容必须满足：

1. 只使用合成数据，不粘贴真实企业、人员、设备、证书或环境信息。
2. 不加入 `.env`、数据库、日志、导出表格、上传文件、私钥或凭据。
3. 新增状态流转或匹配规则时，同步补失败路径、重复提交和冲突测试。
4. 运行 `bash scripts/verify.sh` 并确认通过。
5. 不把私有仓库的提交哈希、远端地址、部署路径或历史记录带入本仓库。
